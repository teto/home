# TODO remove  ? it should already be generated
[user]
	email = "matthieu.coudron@novainsilico.ai"
	name = "Matthieu Coudron"
    signingkey = ~/home/secrets/ssh/nova_key.pub

[gpg]
format =  ssh

[commit]
gpgsign = true
